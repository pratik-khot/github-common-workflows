name: Release Please

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    uses: pratik-khot/github-common-workflows/.github/workflows/release.yml@v1
    secrets: inherit